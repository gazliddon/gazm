{
  "name": "treesitter-gazm",
  "version": "1.0.0",
  "description": "",
  "main": "bindings/node",
  "scripts": {
    "test": "tree-sitter test",
    "build": "tree-sitter generate",
    "build:node": "node-gyp configure build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.15.0",
    "minimatch": ">=3.0.5"
  },
  "devDependencies": {
    "eslint": "^8.8.0",
    "tree-sitter-cli": "^0.20.4",
    "minimatch": ">=3.0.5"
  },

  "tree-sitter": [
    {
      "scope": "source.gazm",
      "file-types": [
        "68",
        "gazm",
        "asm"
      ]
    }
  ]
}
